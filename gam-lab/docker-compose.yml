services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: auditdb
    ports:
      - "5432:5432"
    volumes:
      - audit_data:/var/lib/postgresql/data

volumes:
  audit_data:
